const SITE = 'https://deelmap.com'

export const metadata = {
  title: 'Cookie Policy | DeelMap',
  description: 'How DeelMap uses cookies and similar technologies.',
  alternates: { canonical: `${SITE}/cookie-policy` },
}

export default function Layout({ children }) {
  return children
}
