const SITE = 'https://deelmap.com'

export const metadata = {
  title: 'Terms of Use | DeelMap',
  description: 'The terms and conditions for using DeelMap.',
  alternates: { canonical: `${SITE}/terms-of-use` },
}

export default function Layout({ children }) {
  return children
}
