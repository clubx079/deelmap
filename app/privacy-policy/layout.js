const SITE = 'https://deelmap.com'

export const metadata = {
  title: 'Privacy Policy | DeelMap',
  description: 'How DeelMap collects, uses, and protects your personal information.',
  alternates: { canonical: `${SITE}/privacy-policy` },
}

export default function Layout({ children }) {
  return children
}
